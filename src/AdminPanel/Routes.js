import React, { Fragment } from "react";

const Routes = () => {
  // let { path, url } = useRouteMatch();
  return (
    <Fragment>
      <h2>Hello there</h2>

    </Fragment>
  );
};

export default Routes;
